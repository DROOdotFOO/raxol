# Archived proposal material

Research that has already been consumed. Everything here was an *input* to a
document that is still live in `../in-flight/`, and its findings were ratified
into that document. It is kept for provenance: when someone asks "why is the
contract shaped like this," the answer is in these files.

Nothing here is a current plan. Read `../in-flight/README.md` for what is.

## What moved and why

| Directory | Lines | Was an input to |
|---|---:|---|
| `harness-research/` | 4,796 | `harness-facts-two-perspectives.md`, `harness-synthesis.md`, and through them the freeze contract |
| `harness-research/audit-trail/` | (included above) | the `AF-*` adversarial-audit findings in `harness-freeze-contracts.md` |

The raw briefs are numbered by the round that produced them: `01`-`09` are the
first cohort (frame, leaders, challengers, protocols, permissioning, horror
stories, user voice, eval science), `10`-`16` the storage round, and
`spike-u5-kill.md` the kill-dynamics spike the roadmap marks DONE.

## Citing archived material

The live documents cite this material by tag (`AD-*`, `FI-*`, `F*`, `R*`) and
by path. Those paths are relative and now point here, so the citations still
resolve. If you move a file out of this directory, fix the referring path in
`../in-flight/` in the same commit.
